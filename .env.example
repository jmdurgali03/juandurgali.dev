# Public canonical URL used by metadata, robots.txt, and sitemap.xml.
NEXT_PUBLIC_SITE_URL=https://juandurgali.dev

# SMTP credentials used only by the server-side contact endpoint.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
CONTACT_RECEIVER_EMAIL=
